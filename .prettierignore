pnpm-lock.yaml
uv.lock
node_modules/
.next/
dist/
build/
coverage/
test-results/
playwright-report/
artifacts/

# Generated from the backend OpenAPI document — never hand-formatted.
#
# These are byte-compared against a fresh generation by `pnpm contracts:check`.
# Letting Prettier reformat them would make the drift check fail on every run.
packages/contracts/openapi.json
packages/contracts/src/generated/

# Vendored MapLibre worker, copied verbatim from node_modules on prebuild.
apps/web/public/maplibre/

# Python is formatted by Ruff, not Prettier.
*.py

# Measurement output, written by `pathable coverage`, `pathable evaluate` and the
# geometry inspection. Reformatting it would put a diff in every regeneration
# and the numbers are the point, not the layout. The Kitchener lineage sample is
# the same kind of output, written by `pathable kitchener audit`.
docs/evidence/*.json
docs/evidence/*.geojson
